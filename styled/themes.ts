import { DefaultTheme as BasicTheme } from 'styled-components';

const defaultSpacing = '0.5rem';

const basicTheme: BasicTheme = {
	borderRadius: '0.5rem',
	spacing: defaultSpacing,
	contentsMaxWith: '1200px',

	headerHeight: `3rem`,
};

export { basicTheme };
